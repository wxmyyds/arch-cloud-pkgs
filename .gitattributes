# 这些文件会在 Linux 容器里被 bash / makepkg 执行或 source，行尾必须是 LF。
# 只在 Windows 上靠 core.autocrlf 兜底不够稳（一旦 autocrlf 被关掉，
# 提交进来的 CRLF 会让 bash 报 `\r: command not found`），这里显式钉死。
*.sh        text eol=lf
PKGBUILD    text eol=lf
*.conf      text eol=lf
*.yml       text eol=lf
.gitattributes text eol=lf
